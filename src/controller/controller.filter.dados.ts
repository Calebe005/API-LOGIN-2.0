export default async function FilterData(user:any) {
    /*
    nome_usuario,
    sobre_nome_usuario,
    email_usuario,
    senha_usuario,
    data_nascimento
    */
    console.log(user);

   //if(user.nome_usuario.length || user.sobre_nome_usuario.length > 20){
        

   

}